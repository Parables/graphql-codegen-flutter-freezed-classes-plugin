# GraphQL Code Generator Flutter Freezed Classes Plugin

This is the plugin which is used by the [flutter-freezed-classes](https://www.npmjs.com/package/flutter-freezed-classes) CLI. Install the CLI instead.
